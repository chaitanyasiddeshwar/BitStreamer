// Versions chosen for compatibility as of mid-2026; if Android Studio suggests
// patch upgrades on sync, accept them.
plugins {
    id("com.android.application") version "8.10.1" apply false
    id("org.jetbrains.kotlin.android") version "2.1.20" apply false
}
